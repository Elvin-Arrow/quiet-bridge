# Copy to .env and fill in. .env is gitignored — never commit it.

# LLM — powers signal perception in the care-state router (§11)
OPENAI_API_KEY=sk-proj-...
OPENAI_MODEL=gpt-4o-mini

# Voice — optional. off | elevenlabs   (§3E: off is a complete product)
VOICE_MODE=off
ELEVENLABS_API_KEY=sk_...
ELEVENLABS_VOICE_ID=

PORT=3000
